# 硬件清单与接线

> 完整规格与选型理由见 [`docs/00-总体方案-PRD.md`](../docs/00-总体方案-PRD.md) 第 3 章。

---

## 清单

| 类别 | 型号 | 数量 | 状态 |
|---|---|---|---|
| 主控 | ESP32-S3（YD-ESP32-S3-N16R8，16 MB Flash / 8 MB PSRAM） | 1 | 已有 |
| **CAN 收发器** | **3.3 V CAN 收发模块**（SN65HVD230 / TJA1051T/3） | **1** | **需采购** |
| 惯性测量单元 | ICM-42688-P 六轴 IMU，SPI | 1 | 已有 |
| 执行机构 | ZDT X42S 第二代闭环步进电机（Emm 5.0 固件）×2 | 2 | 已有 |
| 无线手柄 | 标准 BLE HID 手柄 | 1 | 已有 |
| 视觉主控 | 泰山派 RK3576（4×A72 + 4×A53，NPU 6 TOPS） | 1 | 已有（二期） |
| 摄像头 | USB 免驱 1080P，定焦即可 | 1 | 已有（二期） |
| 光学辅助 | 激光笔（视轴稳定性量化用） | 1 | 待确认 |
| 供电 | 12 V 电池 + DC-DC 降压（12 V→5 V） | 1 套 | 部分已有 |
| 终端电阻 | 120 Ω | 2 | 已有 |
| 调试 | USB-CAN 适配器（ZDT_CANable）+ PC | 1 套 | 已有 |

### ⚠️ CAN 收发器采购要点

ESP32-S3 芯片内部有 **TWAI**（CAN 2.0 控制器），但只输出逻辑电平 TX/RX，
**必须外接收发器芯片**才能接上电机总线。现有的 `ZDT_CANable` 是 **USB 适配器**，
插不到 ESP32 上。

- ✅ **SN65HVD230** 首选；**TJA1051T/3** 亦可
- ❌ **不要买 MCP2551** —— 5 V 供电，与 ESP32 电平不兼容，还得额外做电平转换
- 优先选**板载 120 Ω 终端电阻**的模块（视总线拓扑决定是否启用）

---

## 引脚分配

| 功能 | 引脚 | 说明 |
|---|---|---|
| TWAI_TX | GPIO4 | → CAN 收发器 TXD |
| TWAI_RX | GPIO5 | ← CAN 收发器 RXD |
| IMU SPI_SCK | GPIO12 | ICM-42688 SCLK |
| IMU SPI_MOSI | GPIO11 | ICM-42688 SDI |
| IMU SPI_MISO | GPIO13 | ICM-42688 SDO |
| IMU SPI_CS | GPIO10 | 片选 |
| IMU INT1 | GPIO14 | 数据就绪中断（可选，轮询亦可） |
| UART1_TX | GPIO17 | → 泰山派 RX（二期） |
| UART1_RX | GPIO18 | ← 泰山派 TX（二期） |
| 板载 RGB (WS2812) | GPIO48 | 状态指示，无需接线 |
| 板载 BOOT 按键 | GPIO0 | 本地急停 / 模式切换备用 |
| UART0 | GPIO43/44 | 调试日志，保留 |

> SPI 选 **GPIO10–13** 是因为它们是 ESP32-S3 上 **SPI2（FSPI）的默认引脚**，
> 走 ESP-IDF 默认 IO_MUX 路由，避免 GPIO Matrix 引入额外延迟。

### 必须避开的引脚

| 引脚 | 原因 |
|---|---|
| **GPIO26–32** | 板载 Flash / PSRAM 专用，**不可用作普通 IO**，否则系统崩溃 |
| **GPIO33–37** | 8 MB **Octal PSRAM** 可能额外占用（厂商文档只列了 26–32）。**上板前用万用表/原理图确认**，本项目已避开 |
| GPIO19 / 20 | 原生 USB（启用 USB CDC 时被占用） |
| GPIO0 / 45 / 46 | strapping 引脚 |
| GPIO8–15 / 45 / 46 | ADC2，WiFi/BLE 开启时不可用（本项目不用 ADC） |

---

## 接线拓扑

```
                              ┌──────────────────────┐
   USB 摄像头 ──USB──────────►│  泰山派 RK3576       │
   （二期）                    │  Linux + NPU         │
                              └──────────┬───────────┘
                                         │ UART 115200（二期）
                                         │
  ┌──────────────┐   BLE    ┌────────────▼───────────┐
  │  BLE 手柄    │◄────────►│       ESP32-S3         │
  └──────────────┘           │  ESP-IDF + FreeRTOS    │
                              └───┬──────────────┬─────┘
                                  │ TWAI TX/RX   │ SPI（6 线）
                       ┌──────────▼────────┐  ┌──▼─────────────────┐
                       │  3.3V CAN 收发器  │  │ ICM-42688  ★装在   │
                       └──────────┬────────┘  │ 托盘上，随载荷运动 │
                                  │           └────────────────────┘
                     ═════════════╪═════  CAN_H / CAN_L
                        ┌─────────┼─────────┐
                  ┌─────▼─────┐ ┌─▼─────┐ ┌─▼──────────┐
                  │  120 Ω    │ │电机1  │ │电机2 X42S  │  │120 Ω│
                  │ 终端电阻  │ │(Addr1)│ │(Addr=2)    │  │终端 │
                  └───────────┘ └───────┘ └────────────┘  └─────┘
```

---

## 供电方案：12 V 电池

X42S 供电范围 **10–29 V**（标准版），12 V 可行。但电池类型要挑：

| 电池类型 | 电压区间 | 结论 |
|---|---|---|
| 3S LiPo | 满 12.6 V → 放空 9.0 V | ⚠ **必须限制放电深度**，放到 9 V 会低于电机 10 V 下限触发低压保护。建议截止 3.5 V/cell（≈10.5 V） |
| **4S LiFePO4** | 10.0 → 14.6 V（标称 12.8 V） | ✅ **最合适** |
| 铅酸 12 V | 放空 10.5 V → 浮充 13.8 V | ✅ 可用，但重、放电倍率低 |

**12 V 相比 24 V 的两个影响**：

1. **高速扭矩下降** —— 供电电压越低，高速时电流爬升越慢、扭矩衰减越早。
   本项目是低速低负载工况，影响可忽略；**但对增稳环要留意**：
   增稳要在扰动瞬间快速反向纠偏，对瞬时扭矩有要求。
   若发现增稳响应"发软"，优先怀疑 12 V 下的扭矩不足。
2. **电流翻倍** —— 同功率下 12 V 电流是 24 V 的 2 倍，电池放电能力要匹配。

> **电流不用估算，直接测**：`python tools/zdt_can.py status` 会读回总线电压与相电流。
> 让转台跑起来（尤其增稳环频繁纠偏时）看实际电流。

---

## CAN 总线接线要点

- **CAN_H / CAN_L 不能接反**（接反是最常见的"扫不到设备"原因之一）
- **两端各一个 120 Ω 终端电阻**，总线长度尽量短
- **必须共地** —— 电机电源地与 ESP32 地要连
- 波特率 **500 kbps**，扩展帧，`ID = (地址 << 8) | 包号`

### 现场排查的根因优先级

历史排查证明，"读不到数据"的根因按这个顺序找：

1. 电机小屏不显示 + 蓝灯常亮 → 固件卡 bootloader → 重刷
2. 接线松紧 / 接触不良 → 重插压紧
3. **地址重复**
4. `P_Serial` 参数必须 = `CAN1-MAP`
5. CAN_H / CAN_L 接反
6. 共地 + 120 Ω 终端电阻

> 在官方 GUI 里遇到的"地址 2 无数据返回"是 **GUI 侧的问题**，
> 不是硬件或地址问题——`python tools/zdt_can.py --addr 1,2 scan` 实测两台都在线。
> 详见 [`docs/01-CAN调试入门指南.md`](../docs/01-CAN调试入门指南.md)。

---

## IMU 布线要点

ICM-42688-P **装在托盘（载荷）上**，随载荷一起运动——这样测到的姿态不含机构柔性误差。
代价是 **SPI 线要跨转动关节**：

- 一期用**限位自转 ±90°** 规避绕线问题
- 必须有**掉线检测**：IMU 异常时自动旁路稳定环（否则会拿错误数据去纠偏）
- IMU 用 **SPI 不用 I2C**：I2C 上限 1 MHz 且带时钟拉伸与仲裁的不确定性；
  SPI 一次突发读 6 字节在 1 kHz 下耗时 < 10 µs，**延迟确定**。
  增稳环对相位滞后极其敏感，**采样延迟的确定性比平均速度更重要**

---

## 实物照片

`photos/` 下是硬件实拍图。

| 文件 | 内容 |
|---|---|
| `设备总览.jpg` | 当前硬件摊开图：2× X42S 闭环步进 + 斜锥齿轮差速机构（中间白色齿轮盘）、BLE 手柄、ESP32-S3 主控板、USB-CAN 适配器及各模块 |

> **当前阶段是分模块打通、最后一体装配**，所以还是摊开状态没有整机照片。
> 机构装配完成后会补一张整机图。

## 机械

`mechanical/` 下是自制斜锥齿轮云台（V2）的 SolidWorks 模型。
两台电机通过一对斜锥齿轮把运动差速合成俯仰与自转。

> ⚠ **2 号电机是镜像安装的**，世界坐标下需要的转角与编码器读数**符号相反**。
> 这个坑在软件侧的补偿见 [`tools/README.md`](../tools/README.md) 与
> [PRD 3.4.3](../docs/00-总体方案-PRD.md)（已实机验证）。
